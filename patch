--- a/test/test_linux_interop.c
+++ b/test/test_linux_interop.c
@@ -39,6 +39,10 @@
 #include <string.h>
 #include <stdint.h>
 #include <stdbool.h>
+
+// start modified part
+// Linux libax25 library headers (linker: -lax25)
+#include <netax25/ax25.h>
+#include <netax25/axlib.h>
+// end modified part

 #include "ax25.h"
 #include "ax25_state_machine.h"
@@ -2297,123 +2297,245 @@ static int sec_n_libax25_compat(void) {
     uint8_t err;
     size_t enc_len;
     uint8_t *enc;
 
-    // N.1 – "W1AW  -0" (standard SSID-0 form used by libax25)
+    // N.1 – libax25 ax25_aton_entry("W1AW") produces the same 7 bytes
     {
-        // libax25 ax25_aton_entry("W1AW") produces:
-        // 'W'*2  '1'*2  'A'*2  'W'*2  ' '*2  ' '*2  [SSID=0, res=0x60, no ext]
-        uint8_t expected[7] = { 'W' * 2, '1' * 2, 'A' * 2, 'W' * 2, ' ' * 2, ' ' * 2, 0x60 };
+        // start modified part
+        ax25_address libax25_addr;
+        memset(&libax25_addr, 0, sizeof(libax25_addr));
+        int aton_rc = ax25_aton_entry("W1AW", &libax25_addr);
+        TEST_ASSERT(aton_rc == 0, "N.1 libax25 ax25_aton_entry(W1AW) ok", (unsigned)aton_rc);
+        uint8_t *libax25_wire = (uint8_t *)libax25_addr.ax25_call;
+        // end modified part

         ax25_address_t addr;
         memset(&addr, 0, sizeof(addr));
         memcpy(addr.callsign, "W1AW  ", 6);
         addr.ssid = 0;
         addr.ch = false;
         addr.res0 = true;
         addr.res1 = true;
         addr.extension = false;

         enc = ax25_address_encode(&addr, &enc_len, &err);
-        TEST_ASSERT(enc != NULL && err == 0, "N.1 encode W1AW-0", err);
-        COMPARE_FRAME(enc, enc_len, expected, 7, "N.1 W1AW-0 matches libax25 wire format");
+        TEST_ASSERT(enc != NULL && err == 0, "N.1 libax25v22 encode W1AW-0 ok", err);
+        // start modified part
+        int callsign_match = (memcmp(enc, libax25_wire, 6) == 0);
+        TEST_ASSERT(callsign_match, "N.1 callsign bytes match libax25 wire format", 0);
+        TEST_ASSERT((enc[6] & 0x60u) == 0x60u, "N.1 res bits 0x60 set in our encoding", 0);
+        TEST_ASSERT((libax25_wire[6] & 0x60u) == 0x60u, "N.1 res bits 0x60 set in libax25", 0);
+        // end modified part
         free(enc);
     }
 
-    // N.2 – "KA9Q  -3" with extension bit (last address)
+    // N.2 – ax25_aton_entry("KA9Q-3") SSID and extension bit
     {
-        uint8_t expected[7] = { 'K' * 2, 'A' * 2, '9' * 2, 'Q' * 2, ' ' * 2, ' ' * 2,
-            0x60 | (3 << 1) | 0x01 };
+        // start modified part
+        ax25_address libax25_addr;
+        memset(&libax25_addr, 0, sizeof(libax25_addr));
+        int aton_rc = ax25_aton_entry("KA9Q-3", &libax25_addr);
+        TEST_ASSERT(aton_rc == 0, "N.2 libax25 ax25_aton_entry(KA9Q-3) ok", (unsigned)aton_rc);
+        uint8_t libax25_ssid = (libax25_addr.ax25_call[6] >> 1) & 0x0Fu;
+        TEST_ASSERT(libax25_ssid == 3, "N.2 libax25 SSID==3", (unsigned)libax25_ssid);
+        // end modified part

         ax25_address_t addr;
         memset(&addr, 0, sizeof(addr));
         memcpy(addr.callsign, "KA9Q  ", 6);
         addr.ssid = 3;
         addr.ch = false;
         addr.res0 = true;
         addr.res1 = true;
         addr.extension = true;

         enc = ax25_address_encode(&addr, &enc_len, &err);
-        TEST_ASSERT(enc != NULL && err == 0, "N.2 encode KA9Q-3 ext=1", err);
-        COMPARE_FRAME(enc, enc_len, expected, 7, "N.2 KA9Q-3 extension bit libax25 format");
+        TEST_ASSERT(enc != NULL && err == 0, "N.2 libax25v22 encode KA9Q-3 ext=1 ok", err);
+        // start modified part
+        uint8_t our_ssid_field = (enc[6] >> 1) & 0x0Fu;
+        TEST_ASSERT(our_ssid_field == 3, "N.2 our SSID field==3 in wire byte", (unsigned)our_ssid_field);
+        TEST_ASSERT((enc[6] & 0x01u) == 0x01u, "N.2 extension bit set", 0);
+        // end modified part
         free(enc);
     }
 
-    // N.3 – "VE3XYZ-15" command frame (C=1, SSID=15)
+    // N.3 – ax25_ntoa round-trip: encode with libax25v22, decode with libax25
     {
-        uint8_t expected[7] = { 'V' * 2, 'E' * 2, '3' * 2, 'X' * 2, 'Y' * 2, 'Z' * 2,
-            0x80 | 0x60 | (15 << 1) };
-
         ax25_address_t addr;
         memset(&addr, 0, sizeof(addr));
         memcpy(addr.callsign, "VE3XYZ", 6);
         addr.ssid = 15;
         addr.ch = true;
         addr.res0 = true;
         addr.res1 = true;
         addr.extension = false;

         enc = ax25_address_encode(&addr, &enc_len, &err);
-        TEST_ASSERT(enc != NULL && err == 0, "N.3 encode VE3XYZ-15 C=1", err);
-        COMPARE_FRAME(enc, enc_len, expected, 7, "N.3 VE3XYZ-15 C=1 libax25 format");
+        TEST_ASSERT(enc != NULL && err == 0, "N.3 libax25v22 encode VE3XYZ-15", err);
+        // start modified part
+        ax25_address libax25_out;
+        memcpy(libax25_out.ax25_call, enc, 7);
+        const char *ntoa_str = ax25_ntoa(&libax25_out);
+        TEST_ASSERT(ntoa_str != NULL, "N.3 ax25_ntoa returned non-NULL", 0);
+        TEST_ASSERT(strncmp(ntoa_str, "VE3XYZ", 6) == 0, "N.3 ax25_ntoa callsign==VE3XYZ", 0);
+        TEST_ASSERT(strstr(ntoa_str, "-15") != NULL, "N.3 ax25_ntoa includes -15 SSID", 0);
+        // end modified part
         free(enc);
     }
 
-    // N.4 – Minimal AX.25 UI frame wire format check
+    // N.4 – ax25_cmp: two identical addresses compare equal via libax25
     {
-        uint8_t dest_bytes[7] = { 'N' * 2, 'O' * 2, 'C' * 2, 'A' * 2, 'L' * 2, 'L' * 2, 0xE0 };
-        uint8_t src_bytes[7]  = { 'N' * 2, 'O' * 2, 'C' * 2, 'A' * 2, 'L' * 2, 'L' * 2, 0x61 };
+        // start modified part
+        ax25_address a1, a2;
+        memset(&a1, 0, sizeof(a1));
+        memset(&a2, 0, sizeof(a2));
+        ax25_aton_entry("W1AW-5", &a1);
+        ax25_aton_entry("W1AW-5", &a2);
+        int cmp = ax25_cmp(&a1, &a2);
+        // end modified part
+        TEST_ASSERT(cmp == 0, "N.4 ax25_cmp identical addresses==0", (unsigned)cmp);
+    }
 
-        ax25_frame_header_t hdr;
-        memset(&hdr, 0, sizeof(hdr));
-        memcpy(hdr.destination.callsign, "NOCALL", 6);
-        hdr.destination.ssid = 0;
-        hdr.destination.ch = true;
-        hdr.destination.res0 = hdr.destination.res1 = true;
-        memcpy(hdr.source.callsign, "NOCALL", 6);
-        hdr.source.ssid = 0;
-        hdr.source.ch = false;
-        hdr.source.res0 = hdr.source.res1 = true;
-        hdr.source.extension = true;
-        hdr.cr = true;
-        hdr.repeaters.num_repeaters = 0;
-
-        ax25_unnumbered_information_frame_t ui;
-        memset(&ui, 0, sizeof(ui));
-        ui.base.base.type = AX25_FRAME_UNNUMBERED_INFORMATION;
-        ui.base.base.header = hdr;
-        ui.base.pf = false;
-        ui.base.modifier = AX25_U_UI;
-        ui.pid = PID_NO_L3;
-        uint8_t payload[] = { 'H', 'i' };
-        ui.payload = payload;
-        ui.payload_len = sizeof(payload);
-
-        enc = ax25_frame_encode((ax25_frame_t *)&ui, &enc_len, &err);
-        TEST_ASSERT(enc != NULL && err == 0, "N.4 NOCALL UI encode", err);
-        TEST_ASSERT(enc_len >= 16, "N.4 minimum frame length", (unsigned)enc_len);
-        COMPARE_FRAME(enc, 7, dest_bytes, 7, "N.4 dest address bytes");
-        COMPARE_FRAME(enc + 7, 7, src_bytes, 7, "N.4 src address bytes");
-        TEST_ASSERT(enc[14] == 0x03u, "N.4 UI control byte==0x03", enc[14]);
-        TEST_ASSERT(enc[15] == PID_NO_L3, "N.4 PID byte==0xF0", enc[15]);
+    // N.5 – ax25_cmp: different callsigns compare not equal
+    {
+        ax25_address a1, a2;
+        ax25_aton_entry("W1AW-0", &a1);
+        ax25_aton_entry("KA9Q-0", &a2);
+        int cmp = ax25_cmp(&a1, &a2);
+        TEST_ASSERT(cmp != 0, "N.5 ax25_cmp different callsigns!=0", 0);
+    }
 
-        free(enc);
+    // N.6 – ax25_cmp: same callsign different SSID
+    {
+        ax25_address a1, a2;
+        ax25_aton_entry("W1AW-3", &a1);
+        ax25_aton_entry("W1AW-7", &a2);
+        int cmp = ax25_cmp(&a1, &a2);
+        TEST_ASSERT(cmp != 0, "N.6 ax25_cmp same call diff SSID!=0", 0);
     }
 
-    // N.5 – ax25_ntoa equivalent: decode 7 bytes -> callsign string
+    // N.7 – libax25v22 address_decode produces bytes compatible with libax25 ax25_ntoa
     {
         uint8_t raw7[7] = { 'N' * 2, '0' * 2, 'C' * 2, 'A' * 2, 'L' * 2, 'L' * 2, 0x60 };
         ax25_address_t *a = ax25_address_decode(raw7, &err);
-        TEST_ASSERT(a != NULL, "N.5 decode raw 7 bytes", err);
-        TEST_ASSERT(strncmp(a->callsign, "N0CALL", 6) == 0, "N.5 callsign==N0CALL", 0);
-        TEST_ASSERT(a->ssid == 0, "N.5 SSID==0", 0);
-        TEST_ASSERT(a->ch == false, "N.5 C bit==0", 0);
-        ax25_address_free(a, &err);
+        TEST_ASSERT(a != NULL && err == 0, "N.7 address_decode raw7", err);
+        TEST_ASSERT(strncmp(a->callsign, "N0CALL", 6) == 0, "N.7 callsign==N0CALL", 0);
+        TEST_ASSERT(a->ssid == 0, "N.7 SSID==0", 0);
+        // start modified part
+        ax25_address la;
+        memcpy(la.ax25_call, raw7, 7);
+        const char *s = ax25_ntoa(&la);
+        TEST_ASSERT(s != NULL && strncmp(s, "N0CALL", 6) == 0,
+                    "N.7 libax25 ax25_ntoa(raw7)==N0CALL", 0);
+        // end modified part
+        ax25_address_free(a, &err);
     }
 
-    // N.6 – ax25_cmp equivalent: two identical addresses compare equal
+    // N.8 – Minimal AX.25 UI frame wire format check vs libax25 encoding
     {
-        ax25_address_t *a1 = ax25_address_from_string("W1AW-5", &err);
-        ax25_address_t *a2 = ax25_address_from_string("W1AW-5", &err);
-        TEST_ASSERT(a1 && a2, "N.6 two addresses allocated", err);
-        size_t l1, l2;
-        uint8_t *e1 = ax25_address_encode(a1, &l1, &err);
-        uint8_t *e2 = ax25_address_encode(a2, &l2, &err);
-        TEST_ASSERT(l1 == l2 && memcmp(e1, e2, l1) == 0,
-                    "N.6 identical addresses produce identical bytes", 0);
-        free(e1); free(e2);
-        ax25_address_free(a1, &err);
-        ax25_address_free(a2, &err);
+        // start modified part
+        ax25_address la_dest, la_src;
+        ax25_aton_entry("NOCALL-0", &la_dest);
+        ax25_aton_entry("NOCALL-0", &la_src);
+        la_dest.ax25_call[6] |= 0x80u;
+        la_src.ax25_call[6]  |= 0x01u;
+        // end modified part
+
+        ax25_frame_header_t hdr;
+        memset(&hdr, 0, sizeof(hdr));
+        memcpy(hdr.destination.callsign, "NOCALL", 6);
+        hdr.destination.ssid = 0;
+        hdr.destination.ch = true;
+        hdr.destination.res0 = hdr.destination.res1 = true;
+        memcpy(hdr.source.callsign, "NOCALL", 6);
+        hdr.source.ssid = 0;
+        hdr.source.ch = false;
+        hdr.source.res0 = hdr.source.res1 = true;
+        hdr.source.extension = true;
+        hdr.cr = true;
+        hdr.repeaters.num_repeaters = 0;
+
+        ax25_unnumbered_information_frame_t ui;
+        memset(&ui, 0, sizeof(ui));
+        ui.base.base.type = AX25_FRAME_UNNUMBERED_INFORMATION;
+        ui.base.base.header = hdr;
+        ui.base.pf = false;
+        ui.base.modifier = AX25_U_UI;
+        ui.pid = PID_NO_L3;
+        uint8_t payload[] = { 'H', 'i' };
+        ui.payload = payload;
+        ui.payload_len = sizeof(payload);
+
+        enc = ax25_frame_encode((ax25_frame_t *)&ui, &enc_len, &err);
+        TEST_ASSERT(enc != NULL && err == 0, "N.8 NOCALL UI encode", err);
+        TEST_ASSERT(enc_len >= 16, "N.8 minimum frame length", (unsigned)enc_len);
+        // start modified part
+        COMPARE_FRAME(enc,     7, la_dest.ax25_call, 7, "N.8 dest bytes match libax25");
+        COMPARE_FRAME(enc + 7, 7, la_src.ax25_call,  7, "N.8 src bytes match libax25");
+        // end modified part
+        TEST_ASSERT(enc[14] == 0x03u,     "N.8 UI ctrl byte==0x03", enc[14]);
+        TEST_ASSERT(enc[15] == PID_NO_L3, "N.8 PID byte==0xF0",     enc[15]);
+        free(enc);
     }
 
-    // N.7 – ax25_validate equivalent: ensure minimum 17-byte frame check
+    // N.9 – ax25_validate_frame_size: AX.25 §3.9 minimum 17 bytes
     {
-        TEST_ASSERT(ax25_validate_frame_size(17) == true,  "N.7 17-byte frame valid (AX.25 §3.9)", 0);
-        TEST_ASSERT(ax25_validate_frame_size(16) == false, "N.7 16-byte frame invalid", 0);
-        TEST_ASSERT(ax25_validate_frame_size(0)  == false, "N.7 0-byte frame invalid",  0);
+        TEST_ASSERT(ax25_validate_frame_size(17) == true,
+                    "N.9 17-byte frame valid (AX.25 §3.9)", 0);
+        TEST_ASSERT(ax25_validate_frame_size(16) == false,
+                    "N.9 16-byte frame invalid", 0);
+        TEST_ASSERT(ax25_validate_frame_size(0)  == false,
+                    "N.9 0-byte frame invalid",  0);
+    }
+
+    // N.10 – libax25 ax25_validate: check address field bytes produced by us
+    {
+        ax25_address_t addr;
+        memset(&addr, 0, sizeof(addr));
+        memcpy(addr.callsign, "W1AW  ", 6);
+        addr.ssid      = 5;
+        addr.ch        = false;
+        addr.res0      = true;
+        addr.res1      = true;
+        addr.extension = true;
+
+        enc = ax25_address_encode(&addr, &enc_len, &err);
+        TEST_ASSERT(enc != NULL && err == 0, "N.10 encode W1AW-5 for validate", err);
+        // start modified part
+        int valid = ax25_validate((char *)enc);
+        // end modified part
+        TEST_ASSERT(valid != 0, "N.10 libax25 ax25_validate approves our encoding", 0);
+        free(enc);
     }
 
     return 0;
 }
 
 // =========================================================================
 // SECTION O – KISSATTACH / MKISS MULTI-PORT FRAME ROUTING
 // =========================================================================
 
 static int sec_o_kissattach(void) {
     TEST_SECTION("=== SEC-O: kissattach / mkiss Multi-Port Frame Routing ===");
 
     ax25_kiss_ctx_t ctx;
     ax25_kiss_init(&ctx);
     ctx.serial_write = kiss_serial_write;
     ctx.on_frame     = kiss_frame_cb;
     ctx.kiss_mode    = true;
 
-    // O.1 – kissattach style: raw AX.25 frame wrapped in KISS and decoded
+    // O.1 – kissattach style: libax25-formatted AX.25 frame wrapped in KISS
     {
-        uint8_t ax25_raw[] = {
-                'N' * 2, '0' * 2, 'C' * 2, 'A' * 2, 'L' * 2, 'L' * 2, 0xE0,
-                'W' * 2, '1' * 2, 'A' * 2, 'W' * 2, ' ' * 2, ' ' * 2, 0x61,
-                0x03, 0xF0, 'B', 'E', 'A', 'C', 'O', 'N' };
-
-        uint8_t kiss_frame[64];
+        // start modified part
+        ax25_address la_dest, la_src;
+        ax25_aton_entry("N0CALL-0", &la_dest);
+        ax25_aton_entry("W1AW-0",   &la_src);
+        la_dest.ax25_call[6] |= 0x80u;
+        la_src.ax25_call[6]  |= 0x01u;
+
+        uint8_t ax25_raw[20];
+        memcpy(ax25_raw,     la_dest.ax25_call, 7);
+        memcpy(ax25_raw + 7, la_src.ax25_call,  7);
+        ax25_raw[14] = 0x03u;
+        ax25_raw[15] = 0xF0u;
+        ax25_raw[16] = 'B'; ax25_raw[17] = 'E';
+        ax25_raw[18] = 'A'; ax25_raw[19] = 'C';
+        size_t ax25_raw_len = 20;
+        // end modified part
+
+        uint8_t kiss_frame[64];
         kiss_frame[0] = KISS_FEND;
-        kiss_frame[1] = 0x00;
-        memcpy(kiss_frame + 2, ax25_raw, sizeof(ax25_raw));
-        kiss_frame[2 + sizeof(ax25_raw)] = KISS_FEND;
-        size_t kiss_len = 2 + sizeof(ax25_raw) + 1;
+        kiss_frame[1] = 0x00u;
+        memcpy(kiss_frame + 2, ax25_raw, ax25_raw_len);
+        kiss_frame[2 + ax25_raw_len] = KISS_FEND;
+        size_t kiss_len = 2 + ax25_raw_len + 1;

         g_kiss_rx_len  = 0;
         g_kiss_rx_port = 0xFF;
         ax25_kiss_receive_bytes(&ctx, kiss_frame, kiss_len);

-        TEST_ASSERT(g_kiss_rx_len == sizeof(ax25_raw),
-                    "O.1 kissattach: full AX.25 frame received", 0);
+        TEST_ASSERT(g_kiss_rx_len == ax25_raw_len,
+                    "O.1 kissattach: full libax25-format AX.25 frame received", 0);
         TEST_ASSERT(g_kiss_rx_port == 0, "O.1 frame on port 0", 0);
-        COMPARE_FRAME(g_kiss_rx_frame, g_kiss_rx_len, ax25_raw, sizeof(ax25_raw),
-                      "O.1 AX.25 frame content unchanged through KISS");
+        COMPARE_FRAME(g_kiss_rx_frame, g_kiss_rx_len, ax25_raw, ax25_raw_len,
+                      "O.1 AX.25 frame bytes unchanged (libax25 format)");
+
+        // start modified part: cross-validate destination with libax25 ax25_ntoa
+        uint8_t decode_err;
+        ax25_frame_t *frm = ax25_frame_decode(g_kiss_rx_frame, g_kiss_rx_len,
+                                              MODULO128_FALSE, &decode_err);
+        TEST_ASSERT(frm != NULL && decode_err == 0,
+                    "O.1 libax25v22 decode of libax25-format frame ok", decode_err);
+        if (frm != NULL) {
+            size_t re_len;
+            uint8_t *re_enc = ax25_address_encode(&frm->header.destination,
+                                                  &re_len, &decode_err);
+            if (re_enc != NULL && re_len == 7) {
+                ax25_address la_check;
+                memcpy(la_check.ax25_call, re_enc, 7);
+                const char *ntoa = ax25_ntoa(&la_check);
+                TEST_ASSERT(ntoa != NULL && strncmp(ntoa, "N0CALL", 6) == 0,
+                            "O.1 libax25 ax25_ntoa confirms dest==N0CALL", 0);
+                free(re_enc);
+            }
+            ax25_frame_free(frm, &decode_err);
+        }
+        // end modified part
     }
 
-    // O.2 – mkiss port 1 routing (type byte 0x10)
+    // O.2 – mkiss port 1 routing with libax25-encoded payload
     {
-        uint8_t kiss_p1[] = { KISS_FEND, 0x10, 0xAA, 0xBB, KISS_FEND };
+        // start modified part
+        ax25_address la_src;
+        ax25_aton_entry("VE3XYZ-1", &la_src);
+        la_src.ax25_call[6] |= 0x01u;
+        uint8_t kiss_p1[2 + 7 + 1];
+        kiss_p1[0] = KISS_FEND; kiss_p1[1] = 0x10u;
+        memcpy(kiss_p1 + 2, la_src.ax25_call, 7);
+        kiss_p1[2 + 7] = KISS_FEND;
+        // end modified part
         g_kiss_rx_len  = 0;
         g_kiss_rx_port = 0xFF;
-        ax25_kiss_receive_bytes(&ctx, kiss_p1, sizeof(kiss_p1));
+        ax25_kiss_receive_bytes(&ctx, kiss_p1, 2 + 7 + 1);
         TEST_ASSERT(g_kiss_rx_port == 1, "O.2 mkiss port 1 detected", 0);
+        TEST_ASSERT(g_kiss_rx_len == 7, "O.2 mkiss port 1 payload length", 0);
     }
 
-    // O.3 – mkiss port 7 routing (type byte 0x70)
+    // O.3 – mkiss port 7 routing with libax25-encoded payload
     {
-        uint8_t kiss_p7[] = { KISS_FEND, 0x70, 0xCC, KISS_FEND };
+        // start modified part
+        ax25_address la_src;
+        ax25_aton_entry("KA9Q-7", &la_src);
+        la_src.ax25_call[6] |= 0x01u;
+        uint8_t kiss_p7[2 + 7 + 1];
+        kiss_p7[0] = KISS_FEND; kiss_p7[1] = 0x70u;
+        memcpy(kiss_p7 + 2, la_src.ax25_call, 7);
+        kiss_p7[2 + 7] = KISS_FEND;
+        // end modified part
         g_kiss_rx_len  = 0;
         g_kiss_rx_port = 0xFF;
-        ax25_kiss_receive_bytes(&ctx, kiss_p7, sizeof(kiss_p7));
+        ax25_kiss_receive_bytes(&ctx, kiss_p7, 2 + 7 + 1);
         TEST_ASSERT(g_kiss_rx_port == 7, "O.3 mkiss port 7 detected", 0);
     }
 
     // O.4 – Consecutive frames in same byte stream (inter-frame gap)
     {
-        uint8_t stream[] = {
-                KISS_FEND, 0x00, 0x11, 0x22, KISS_FEND,
-                KISS_FEND, 0x00, 0x33, 0x44, KISS_FEND };
+        // start modified part
+        ax25_address la1, la2;
+        ax25_aton_entry("W1AW-0", &la1);
+        ax25_aton_entry("W1AW-0", &la2);
+        la1.ax25_call[6] |= 0x01u;
+        la2.ax25_call[6] |= 0x01u;
+        uint8_t stream[2*(2+7+1)];
+        size_t off = 0;
+        stream[off++] = KISS_FEND; stream[off++] = 0x00u;
+        memcpy(stream + off, la1.ax25_call, 7); off += 7;
+        stream[off++] = KISS_FEND;
+        stream[off++] = KISS_FEND; stream[off++] = 0x00u;
+        memcpy(stream + off, la2.ax25_call, 7); off += 7;
+        stream[off++] = KISS_FEND;
+        // end modified part
         ax25_kiss_stats_t before, after;
         ax25_kiss_get_stats(&ctx, &before);
-        ax25_kiss_receive_bytes(&ctx, stream, sizeof(stream));
+        ax25_kiss_receive_bytes(&ctx, stream, off);
         ax25_kiss_get_stats(&ctx, &after);
         TEST_ASSERT(after.rx_frames - before.rx_frames == 2,
-                    "O.4 two consecutive frames decoded", 0);
+                    "O.4 two consecutive libax25-format frames decoded", 0);
     }
 
     // O.5 – TXDELAY command received from TNC
     {
-        uint8_t txd_cmd[] = { KISS_FEND, 0x01, 50, KISS_FEND };
+        uint8_t txd_cmd[] = { KISS_FEND, 0x01u, 50u, KISS_FEND };
         g_kiss_rx_len = 0;
         ax25_kiss_receive_bytes(&ctx, txd_cmd, sizeof(txd_cmd));
         TEST_ASSERT(g_kiss_rx_len == 0, "O.5 TXDELAY cmd: no data callback", 0);
     }
 
-    // O.6 – kissnetd style: KISS frame with port reassignment
+    // O.6 – kissnetd style: port 2 with libax25-encoded payload
     {
-        uint8_t kn_frame[] = { KISS_FEND, 0x20, 0xDE, 0xAD, 0xBE, KISS_FEND };
+        // start modified part
+        ax25_address la;
+        ax25_aton_entry("VK2RZ-0", &la);
+        la.ax25_call[6] |= 0x01u;
+        uint8_t kn[2 + 7 + 1];
+        kn[0] = KISS_FEND; kn[1] = 0x20u;
+        memcpy(kn + 2, la.ax25_call, 7);
+        kn[2 + 7] = KISS_FEND;
+        // end modified part
         g_kiss_rx_port = 0xFF;
-        ax25_kiss_receive_bytes(&ctx, kn_frame, sizeof(kn_frame));
+        ax25_kiss_receive_bytes(&ctx, kn, 2 + 7 + 1);
         TEST_ASSERT(g_kiss_rx_port == 2, "O.6 kissnetd port 2", 0);
     }
 
     // O.7 – ax25_kiss_reset_rx clears in-progress frame
     {
-        uint8_t partial[] = { KISS_FEND, 0x00, 0xAA, 0xBB };
+        uint8_t partial[] = { KISS_FEND, 0x00u, 0xAAu, 0xBBu };
         ax25_kiss_receive_bytes(&ctx, partial, sizeof(partial));
         ax25_kiss_reset_rx(&ctx);
         TEST_ASSERT(ctx.rx_state == KISS_RX_IDLE,
                     "O.7 rx_state IDLE after reset", 0);
         TEST_ASSERT(ctx.rx_len == 0, "O.7 rx_len==0 after reset", 0);
     }
 
+    // O.8 – Round-trip: libax25 encode -> libax25v22 decode -> re-encode -> compare
+    {
+        // start modified part
+        ax25_address la_ref;
+        ax25_aton_entry("ZL2BAC-9", &la_ref);
+
+        uint8_t decode_err;
+        ax25_address_t *decoded = ax25_address_decode(
+            (const uint8_t *)la_ref.ax25_call, &decode_err);
+        TEST_ASSERT(decoded != NULL && decode_err == 0,
+                    "O.8 decode libax25 wire bytes with libax25v22", decode_err);
+        TEST_ASSERT(decoded->ssid == 9, "O.8 SSID==9", (unsigned)decoded->ssid);
+
+        size_t re_len;
+        uint8_t *re_enc = ax25_address_encode(decoded, &re_len, &decode_err);
+        TEST_ASSERT(re_enc != NULL && re_len == 7, "O.8 re-encode length==7", 0);
+
+        int match = (memcmp(re_enc, la_ref.ax25_call, 6) == 0);
+        TEST_ASSERT(match, "O.8 callsign bytes after round-trip match libax25", 0);
+        uint8_t our_ssid = (re_enc[6] >> 1) & 0x0Fu;
+        uint8_t la_ssid  = (la_ref.ax25_call[6] >> 1) & 0x0Fu;
+        TEST_ASSERT(our_ssid == la_ssid,
+                    "O.8 SSID nibble matches libax25 after round-trip", 0);
+
+        free(re_enc);
+        ax25_address_free(decoded, &decode_err);
+        // end modified part
+    }
+
     return 0;
 }
